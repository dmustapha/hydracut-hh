# HydraCut Build Codebase Manifest

Generated after Build Phase 0 verification. This is an inventory pointer, not application source.

| Area | Current contents | Build status |
|---|---|---|
| Root toolchain | `package.json`, `pnpm-workspace.yaml`, `pnpm-lock.yaml`, `tsconfig.json`, `next.config.ts`, `vitest.config.ts`, `playwright.config.ts` | Phase 0 verified |
| Runtime | `Dockerfile`, `docker-compose.yml`, `Caddyfile`, ignored `secrets/` | Phase 0 verified |
| Evidence | `docs/evidence/build-toolchain.txt` and prior immutable evidence | Phase 0 verified |
| Application source | No `src/`, `scripts/`, or `tests/` files | Begins in Phase 1 |
| Graph proof | No application adapter yet | Phase 2 and Phase 3 |
| UI | No application route yet | Phase 4 |

## Verification anchor

Phase 0 independently verified frozen pnpm installation, TypeScript 7.0.2, digest-pinned HydraDB, healthy PostgreSQL, private ports, mode `0600` secrets, and the `linux/amd64` dependencies stage. The full application/runtime image remains deferred until source exists.
